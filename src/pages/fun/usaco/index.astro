---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getUSACOEntries } from '../../../utils/content';

const entries = getUSACOEntries();
const difficulties = ['Platinum', 'Gold', 'Silver', 'Bronze'];
---

<BaseLayout title="USACO / Competitive Programming | Jasmer Singh Sanjotra" description="Competitive programming solutions and insights.">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <a href="/fun" class="text-muted hover:text-accent mb-6 inline-flex items-center space-x-2">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      <span>Back to Fun Stuff</span>
    </a>
    
    <header class="page-header">
      <h1 class="page-title">USACO / Competitive Programming</h1>
      <p class="page-subtitle">
        I like to do USACO sometimes. Here's a (probably incomplete) list of whatever I solved.
      </p>
    </header>
    
    <!-- Difficulty Filter -->
    <div class="mb-8">
      <div class="flex flex-wrap gap-2">
        <button
          data-difficulty="all"
          class="difficulty-filter px-4 py-2 rounded-lg border border-divider bg-card text-ink hover:border-accent transition-colors active"
        >
          All
        </button>
        {difficulties.map((diff) => (
          <button
            data-difficulty={diff}
            class="difficulty-filter px-4 py-2 rounded-lg border border-divider bg-card text-ink hover:border-accent transition-colors"
          >
            {diff}
          </button>
        ))}
      </div>
    </div>
    
    <!-- Entries List -->
    <div class="space-y-4" id="entries-list">
      {entries.map((entry) => (
        <a
          href={`/fun/usaco/${entry.slug}`}
          class="entry-card block p-6 border border-divider rounded-lg bg-card hover:border-accent transition-shadow duration-200 hover:shadow-md"
          data-difficulty={entry.difficulty}
        >
          <div class="flex justify-between items-start mb-2">
            <div>
              <h2 class="text-xl font-semibold text-ink">
                {entry.problemName}
              </h2>
              <p class="text-sm text-muted mt-1">
                {entry.contest}
              </p>
            </div>
            <span class="px-3 py-1 text-xs bg-subtle text-ink rounded">
              {entry.difficulty}
            </span>
          </div>
          <p class="text-ink mt-3">
            <strong>Key Idea:</strong> {entry.keyIdea}
          </p>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>

<script>
  const difficultyFilters = document.querySelectorAll('.difficulty-filter');
  const entryCards = document.querySelectorAll('.entry-card');
  
  difficultyFilters.forEach((filter) => {
    filter.addEventListener('click', () => {
      const selectedDifficulty = filter.getAttribute('data-difficulty');
      
      // Update active state
      difficultyFilters.forEach((f) => f.classList.remove('active'));
      filter.classList.add('active');
      
      // Filter entries
      entryCards.forEach((card) => {
        const difficulty = card.getAttribute('data-difficulty');
        if (selectedDifficulty === 'all' || difficulty === selectedDifficulty) {
          card.style.display = 'block';
        } else {
          card.style.display = 'none';
        }
      });
    });
  });
</script>

<style>
  .difficulty-filter.active {
    @apply border-accent font-semibold;
  }
</style>

